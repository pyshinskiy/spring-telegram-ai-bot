telegram:
  bot:
    username: OpenAIProxyPyshBot
    token: ${BOT_TOKEN}

open-router:
  api-key: ${API_KEY}
  provisioning-key: ${PROVISIONING_KEY}

actuate-job:
  url: ${ACTUATE_URL}

management:
  endpoints:
    web:
      exposure:
        include: refresh
